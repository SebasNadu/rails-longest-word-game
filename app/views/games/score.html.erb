<h1>Result</h1>
<% @word.split('').each do |letter| %>
  <% @count += 1 if @letters.include?(letter) %>
<% end %>
<% if @word.split('').length != @count %>
  <p>Sorry but <strong><%= @word %></strong> can't be built out of <%= @example %>.</p>
<% elsif @found %>
  <p><strong>Congratulations! </strong><%= @word %> is a valid English word!</p>
  <% session[:score] += @word.length %>
<% else %>
  <p>Sorry but <strong><%= @word %></strong> does not seem to be a valid English word..</p>
<% end %>
<h3>score: <%= session[:score] %></h3>
<%= link_to 'Play again', new_path %>
